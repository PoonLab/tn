
#' Create an implementation of a graph. This consists of an edge list (TN93, or patristic) and some sequence meta data
#' NOTE: A subset of these sequences are expected to be new .
create.graph <- function(seq.info, edge.info) {
  #'@param seq.info: A set of sequence meta-data sorted by alignment header
  #'@param edge.info: A pairwise edgelist of all associated headers associated with some meta data.
  #'@return: A graph, with sequences and edge info. New sequences are only linked by their minimum retrospective edge
  
  return(graph)
}